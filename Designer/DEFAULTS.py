# general color shortcuts that can be used by designer
BLACK = (0, 0, 0)
SKY_BLUE = (43, 204, 236)
GRASS_GREEN = (50, 120, 60)

# the default colors for items used by designer
platform_color = GRASS_GREEN
background_color = SKY_BLUE

# the default dimensions for the world, screen, floor, etc...
screen_size = [1200, 600]
world_size = [screen_size[0] * 2, screen_size[1]]
floor_height = 20



